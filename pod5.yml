kind: Pod
apiVersion: v1
metadata:
  name: labelpod1
  labels:
    day: fri
    class: kubernetes

spec:
  containers:
  - name: resource
    image: ubuntu
    command: ["/bin/bash", "-c", "while true; do echo Hello-sai; sleep 10; done"]


